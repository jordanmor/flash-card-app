doctype html
html(lang="en")
	head
		title Flash Cards
	body
		include includes/header.pug
		if name
			h2 Welcome, #{name}!
			form(action='/exit' method='post')
				button(type='submit') Exit Flash Cards App
		block content
		include includes/footer.pug
